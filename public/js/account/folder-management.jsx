import React from 'react';
import Folder from './folder';

class FolderManagement extends React.Component {

  render() {
    // TODO: Loop through state of arrays
    //       For each, push a Folder component containing folder info to an array
    //       Return the final array within the <ul>
    return(
      <ul>
        <h4>Folder List</h4>
      </ul>
    )
  }
}

module.exports = FolderManagement;
